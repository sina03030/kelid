import 'package:flutter/material.dart';

class PostPriceAndAreaPage extends StatelessWidget {
  const PostPriceAndAreaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
