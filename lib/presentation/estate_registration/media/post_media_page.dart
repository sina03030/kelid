import 'package:flutter/material.dart';

class PostMediaPage extends StatelessWidget {
  const PostMediaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
