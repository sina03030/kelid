import 'package:flutter/material.dart';

class PostInformationPage extends StatelessWidget {
  const PostInformationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
